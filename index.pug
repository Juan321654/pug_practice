doctype html 
html 
  head 
  body 
    h1 welcome! 
    p. 
      This is a paragraph 
      and this is a new line
    p 
      | adding a pipe makes a new line aswell
      | second line, no need for the 'dot'
    ul 
      li this is item #1
      li this is item #2
      li this is item #3
    h5#pageTitle adding an id
    p.pagePara added a class 
    .display-box this will create a div with a class
    .display-box. 
      this will create a div with a class
      and multilined
    div attributes:
      a(href="https://juancamachoportfolio.netlify.app/") Portfolio
      link(rel='stylesheet' href="style.css")
      input(type="text")
    div(class="anotherWay") to assing class
// declaring javascript vars
- let firstName = "Juan"
- let lastName = "Camacho"
- let fullName = `${firstName} ${lastName}`

p #{firstName}
p.first-name #{firstName}
#first-name #{firstName}
// the equals sign must not contain a space
p= firstName  
#first-name= firstName
.full-name= firstName + " " + lastName
.full-name= `first name: ${firstName}, last name: ${lastName}`
p#full-name= fullName